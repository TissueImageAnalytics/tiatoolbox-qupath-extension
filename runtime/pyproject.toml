[project]
name = "qupath-tiatoolbox-runtime"
version = "0.3.0"
description = "Runtime env for the TIAToolbox QuPath extension (uv-managed)."
requires-python = "==3.11.*"
dependencies = [
    "qupath-tiatoolbox",
]

[tool.uv.sources]
qupath-tiatoolbox = { path = "./sidecar" }
